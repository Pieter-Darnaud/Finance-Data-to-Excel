# Python virtual environment (rebuildable from requirements.txt — never commit)
.venv/

# Generated output (produced by running the script, not source)
*.xlsx

# macOS junk
.DS_Store

# Personal VS Code workspace config
*.code-workspace

# Byte-compiled / cache
__pycache__/
*.pyc

# Scratch / experiments (kept locally, not committed)
Tester.py
